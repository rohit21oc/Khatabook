<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Khaatabook</title>
    <link
    href="https://cdn.jsdelivr.net/npm/remixicon@4.3.0/fonts/remixicon.css"
    rel="stylesheet"
/>
     <style>
        body {
    font-family: Arial, sans-serif;
    background-color: #f4f4f4;
    margin: 0;
    padding: 0;
}

.navbar {
    display: flex;
    justify-content: space-between;
    align-items: center;
    background-color: #fff;
    padding: 10px 20px;
    box-shadow: 0px 2px 4px rgba(0, 0, 0, 0.1);
}

.logo {
    font-size: 24px;
    font-weight: bold;
}

.nav-links a {
    margin-left: 20px;
    text-decoration: none;
    color: #333;
    font-size: 16px;
}

.container {
    max-width: 800px;
    margin: 20px auto;
    padding: 20px;
    background-color: #fff;
    box-shadow: 0px 2px 4px rgba(0, 0, 0, 0.1);
}

h2 {
    font-size: 24px;
    margin-bottom: 20px;
}
h3 {
    font-size: 12px;
    color: #00000057;

}

.record-list {
    display: flex;
    flex-direction: column;
}

.record {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 10px;
}

.date-button {
    background-color: #007bff;
    color: #fff;
    border: none;
    padding: 10px 20px;
    font-size: 16px;
    border-radius: 5px;
    cursor: pointer;
}

.actions {
    display: flex;
    align-items: center;
}

.edit-button, .delete-button {
    background-color: transparent;
    border: none;
    cursor: pointer;
    font-size: 18px;
    margin-left: 10px;
}
.edit-button a{
    text-decoration: none;
    color: green;
}

.edit-button:hover, .delete-button:hover {
    color: #007bff;
}
a{
    text-decoration: none;
    color: black;
}

     </style>

</head>
<body>
    <div class="navbar">
        <div class="logo"> <a href="/">Khaatabook</a> </div>
        <div class="nav-links">
            <a href="/">Home</a>
            <a href="/create">Create New Hisaab</a>
        </div>
    </div>

    <div class="container">
        <h2>All Hisaab Kitaab</h2>
        <div class="record-list">

            <% if(files.length>0 ){ %>
                <% files.forEach(function(file){ %>
                    <div class="record">
                        <button class="date-button"> <%= file.split(".")[0] %> <a href="/hisaab/<%=file %>"><i class="ri-arrow-right-circle-fill"></i></a> </button>
                        <div class="actions">
                            <button class="edit-button"> <a href="/edithisaab/<%= file %>"><i class="ri-pencil-line"></i></a> </button>
                            <button class="delete-button"> <a href="/delete/<%= file %>"><i class="ri-delete-bin-6-line"></i></a> </button>
                        </div>
                    </div>
              <%  })%>
            <% } else{ %>
                <h3>Curruntly nothing to show hisaab</h3>
            <% } %>
        </div>
    </div>
</body>
</html>
